#include"mine.h"

int main(){
    ptr+=init;
    loader();
    int c=checker();
    if(c){
        cerr<<"Syntax error!! PLZ check!!";
        exit(5);
    }
    runner();
    getch();
    return 0;
}
